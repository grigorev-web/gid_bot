# Дефолтный npm registry
registry=https://registry.npmjs.org/

# Дополнительные настройки (раскомментируйте при необходимости)
# @your-scope:registry=https://npm.pkg.github.com/
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# Настройки для приватных пакетов
# @your-company:registry=https://npm.your-company.com/
# //npm.your-company.com/:_authToken=${NPM_TOKEN}

# Настройки прокси (если нужно)
# proxy=http://proxy.company.com:8080
# https-proxy=http://proxy.company.com:8080

# Настройки для кэширования
cache=~/.npm 